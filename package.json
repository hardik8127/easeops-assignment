{
  "name": "easeops-elibrary",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "heroku-postbuild": "npm install --prefix frontend && ./frontend/node_modules/.bin/vite build --config frontend/vite.config.ts"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  }
}
